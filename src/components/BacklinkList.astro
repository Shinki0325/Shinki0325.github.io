---
type BacklinkItem = {
  href: string;
  title: string;
  kind: string;
};

interface Props {
  items: BacklinkItem[];
}

const { items } = Astro.props;
---

<section class="glass-panel side-panel stack">
  <h2>引用此页</h2>
  {
    items.length > 0 ? (
      <ul class="backlinks">
        {items.map((item) => (
          <li>
            <a href={item.href}>{item.title}</a>
            <span class="muted"> {item.kind}</span>
          </li>
        ))}
      </ul>
    ) : (
      <p class="muted">暂时还没有公开页面引用这里，等主稿和笔记继续补链后会出现在这里。</p>
    )
  }
</section>

<style>
  .backlinks {
    display: grid;
    gap: 0.75rem;
    margin: 0;
    padding-left: 1.25rem;
  }
</style>
