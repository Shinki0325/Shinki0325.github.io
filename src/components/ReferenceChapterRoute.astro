---
import type { ReferenceChapter } from "../lib/reference-publication";

interface Props { chapters: ReferenceChapter[] }
const { chapters } = Astro.props;
const total = String(chapters.length).padStart(2, "0");
---

{chapters.length > 0 && (
  <nav class="reference-chapter-directory" aria-label="本文目录" data-reference-chapter-directory>
    <header class="reference-chapter-directory__header">
      <strong>阅读目录</strong><span>{total} CHAPTERS</span>
    </header>
    <ol class="reference-chapter-directory__list">
      {chapters.map((chapter) => (
        <li>
          <a
            href={`#${chapter.id}`}
            data-reference-chapter-link
            data-chapter-id={chapter.id}
          >
            <span>{String(chapter.number).padStart(2, "0")}</span>
            <span><strong>{chapter.titleZh}</strong>{chapter.summaryZh && <small>{chapter.summaryZh}</small>}</span>
          </a>
        </li>
      ))}
    </ol>
  </nav>
)}
